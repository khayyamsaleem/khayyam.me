version: '2'

services:
  personalsite:
    build: .
    networks:
      - web
    labels:
      - "traefik.backend=personalsite"
      - "traefik.frontend.rule=Host: khayyam.me, www.khayyam.me"
      - "traefik.port=1313"
    restart: always

networks:
  web:
    external:
      name: reverse-proxy_webgateway
